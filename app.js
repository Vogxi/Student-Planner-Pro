window.addEventListener("DOMContentLoaded", () => {

    initializeNavigation();

    initializeTimetable();

    initializeHomework();

    initializeDashboard();

    initializeExams();

    initializeCalendar();

    initializeTimer();

    initializeSettings();

    updateStatistics();

    console.log("Student Planner Pro Loaded");

});